import styled from "styled-components";

export const Ins = styled.ins`
    font-family: inherit;
    font-size: inherit;
    color: inherit;
    font-weight: inherit;
    text-decoration: inherit;
`;